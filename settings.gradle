pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://repo.spring.io/release' }
    }
}

rootProject.name = 'wallet_tests'
include 'multi-source-test-client'
